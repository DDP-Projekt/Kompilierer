Binde "Duden/Ausgabe" ein.

[alias the type]
Wir nennen eine Zahl auch eine Hausnummer.

[an alias can be assigned it's underlying type]
Die Hausnummer h ist 22.

Schreibe h auf eine Zeile.
Schreibe (h plus 2) auf eine Zeile.

Die Zahl z ist h.
Schreibe z auf eine Zeile.

Schreibe (h als Zahl) auf eine Zeile.
Schreibe (z als Hausnummer) auf eine Zeile.

Schreibe (h als Text) auf eine Zeile.

Die Funktion foo mit dem Parameter h vom Typ Hausnummer, gibt eine Hausnummer zurück, macht:
	Gib h plus 2 zurück.
Und kann so benutzt werden:
	"foo <h>"

Schreibe (foo h) auf eine Zeile.

Die Funktion bar mit dem Parameter z vom Typ Zahlen Liste, gibt eine Zahl zurück, macht:
	Gib z an der Stelle 1 zurück.
Und kann so benutzt werden:
	"bar <z>"

Die Hausnummer Liste nummern ist eine Liste, die aus 1, 2, 3 besteht.

Schreibe (bar nummern) auf eine Zeile.

Erhöhe h um 1.

Schreibe h auf eine Zeile.

[
	[ Should error, because the alias already exists for Schreibe_Zahl ]
	Die Funktion Schreibe_Hausnummer mit dem Parameter hnummer vom Typ Hausnummer, gibt nichts zurück, macht:
		Schreibe (hnummer als Zahl) auf eine Zeile.
		Schreibe "hi" auf eine Zeile.
	Und kann so benutzt werden:
	"Schreibe <hnummer>"
]
