Binde "Duden/Ausgabe" ein.

[simple struct declaration with all alias parameter combinations]
Wir nennen die Kombination aus
	der Zahl x mit Standardwert 0, [private field]
	der öffentlichen Zahl y mit Standardwert 0, [public field]
einen Vektor2, und erstellen sie so:
	"Nullvektor2" oder
	"der Nullvektor2" oder [0 parameters]
	"ein Vektor2 mit x gleich <x>" oder [1 parameter]
	"ein Vektor2 mit x gleich <x> und y gleich <y>" [all parameters]

Die Funktion Schreibe_Vektor mit dem Parameter vek vom Typ Vektor2, gibt nichts zurück, macht:
	Schreibe "x: ".
	Schreibe (x von vek).
	Schreibe " y: ".
	Schreibe (y von vek).
Und kann so benutzt werden:
	"Schreibe <vek>" oder
	"Schreibe den Vektor2 <vek>"

Die Funktion Verändere_Vektor mit dem Parameter vek vom Typ Vektor2 Referenz, gibt nichts zurück, macht:
	Speichere (x von vek) plus 1 in x von vek.
	Speichere (y von vek) plus 1 in y von vek.
Und kann so benutzt werden:
	"Verändere <vek>" oder
	"Verändere den Vektor2 <vek>"

Die Funktion Verändere_Vektor_Pur mit dem Parameter vek vom Typ Vektor2, gibt einen Vektor2 zurück, macht:
	Gib ein Vektor2 mit x gleich ((x von vek) plus 1) und y gleich ((y von vek) plus 1) zurück.
Und kann so benutzt werden:
	"Veränderter <vek>"

Der Vektor2 vek ist der Nullvektor2.
Schreibe vek.
Schreibe '\n'.
Verändere vek.
Schreibe vek.
Schreibe '\n'.
Schreibe (Veränderter vek).
Schreibe '\n'.