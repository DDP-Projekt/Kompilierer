Binde "Duden/Ausgabe" ein.

Wir nennen die Kombination aus
	der Zahl z mit Standardwert 0,
eine Struktur, und erstellen sie so:
	"eine Struktur mit z gleich <z>"

Die generische Funktion foo mit den Parametern a und b vom Typ T und T, gibt eine Zahl zurück, macht:
	Gib z von a plus z von b zurück.
Und überlädt den "plus" Operator.

Die Struktur a ist eine Struktur mit z gleich 2.
Die Struktur b ist eine Struktur mit z gleich 3.

Schreibe (a plus b) auf eine Zeile.
