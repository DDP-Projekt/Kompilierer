Binde "Duden/Ausgabe" ein.
Binde "decl" ein.

Schreibe (x von vec2_zahl) auf eine Zeile.
Schreibe (y von vec2_zahl) auf eine Zeile.

Schreibe (x von vec2_text) auf eine Zeile.
Schreibe (y von vec2_text) auf eine Zeile.

Schreibe '\n'.

Der Text-Vektor2 vec2_text2 ist Vektor2("x2", "y2").
Der Kommazahl-Vektor2 vec2_kommazahl ist Vektor2(1,5, 2,3).

Schreibe (x von vec2_text2) auf eine Zeile.
Schreibe (y von vec2_text2) auf eine Zeile.

Schreibe (x von vec2_kommazahl) auf eine Zeile.
Schreibe (y von vec2_kommazahl) auf eine Zeile.
